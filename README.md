# Français en Belgique 🇧🇪

Веб-приложение для изучения французского языка с акцентом на бельгийские особенности.

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- pip

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd FF_Gemini
```

2. **Создайте виртуальное окружение**
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**

Создайте файл `.env` в корне проекта:
```env
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
```

5. **Примените миграции**
```bash
python manage.py migrate
```

6. **Загрузите начальный контент**
```bash
python manage.py load_initial_content
```

7. **Создайте суперпользователя (опционально)**
```bash
python manage.py createsuperuser
```

8. **Запустите сервер**
```bash
python manage.py runserver
```

Откройте браузер по адресу: `http://127.0.0.1:8000`

## 📁 Структура проекта

```
FF_Gemini/
├── lessons/                 # Основное приложение
│   ├── management/         # Django команды
│   ├── migrations/         # Миграции БД
│   ├── static/lessons/     # CSS, JS файлы
│   ├── templates/          # HTML шаблоны
│   ├── models.py          # Модели данных
│   └── views.py           # Представления
├── project_valerie/        # Настройки проекта
├── .env                    # Переменные окружения (не в git)
├── requirements.txt        # Зависимости Python
└── manage.py              # Django CLI
```

## 🎨 Особенности

- ✅ Адаптивный дизайн (Desktop + Mobile)
- ✅ Современный UI с glassmorphism эффектами
- ✅ Динамический контент из БД
- ✅ Кастомные страницы ошибок (404, 500)
- ✅ Безопасное хранение секретов

## ⌨️ Горячие клавиши

- `→` / `Arrow Right`: Следующий урок
- `←` / `Arrow Left`: Предыдущий урок
- `/` (Slash): Быстрый поиск
- `Esc`: Закрыть меню / Сбросить фокус


## 🛠️ Технологии

- **Backend**: Django 4.1.7
- **Frontend**: HTML, CSS, JavaScript
- **Database**: SQLite
- **Deployment**: Ready for production

## 📝 Управление контентом

Для редактирования уроков используйте Django Admin:
```
http://127.0.0.1:8000/admin
```

## 🔒 Безопасность

- SECRET_KEY хранится в `.env` файле
- DEBUG отключен в продакшене
- ALLOWED_HOSTS настроен для конкретных доменов

## 📄 Лицензия

Proprietary - All rights reserved

## 👨‍💻 Автор

Project Valerie Team
